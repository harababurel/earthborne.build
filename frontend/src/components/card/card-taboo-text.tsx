// Stub — ER has no taboo list.
import type { Card } from "@arkham-build/shared";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function CardTabooText(_props: { card: Card }) {
  return null;
}
