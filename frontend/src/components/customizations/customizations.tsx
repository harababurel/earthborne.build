// Stub — ER has no customization system.
import type { ResolvedCard } from "@/store/lib/types";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function Customizations(_props: { card: ResolvedCard["card"] }) {
  return null;
}
