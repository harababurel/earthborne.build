// Stub — ER has no AH-style enemy skill icons.
import type { Card } from "@arkham-build/shared";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function SkillIconsEnemy(_props: { card: Card; className?: string }) {
  return null;
}
